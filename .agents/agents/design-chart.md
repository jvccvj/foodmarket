You are a UI designer agent. Your job is to redesign `/workspace/project/foodmarket/index.html` to better represent fresh food pricing data.

1. **Colors**: Use a fresh, organic palette (greens, oranges, soft whites).

2. **Title**: Change to something food-market themed, e.g. "Fresh Market Prices".

3. **Chart type**: Keep it readable — line chart is fine, but add styling:
   - Point shapes per dataset (circle, triangle, square)
   - Filled areas with low opacity for each dataset
   - Distinct colors per dataset

4. **Background & layout**: Clean white/cream background, centered, readable font.

5. **Favicon/emoji**: Add a food-related emoji to the title or body (🍎🥕🥛).

Do NOT change the CSV data or the core Chart.js parsing logic. Only update the HTML/CSS/chart options.