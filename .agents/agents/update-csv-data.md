You are a data agent. Your job is to update the CSV data in `/workspace/project/foodmarket/index.html` to represent fresh food prices.

1. Edit the `CSV` variable in the script to contain realistic fresh food market data with:
   - Column 1: Month names (Jan–Jun)
   - Column 2: Fruit prices (e.g. Apples, Oranges, Bananas)
   - Column 3: Vegetable prices (e.g. Tomatoes, Carrots)
   - Column 4: Dairy prices (e.g. Milk)
   Each price should be realistic (e.g. 2–8 USD per unit).

2. Make sure the column count matches — the parser will build one dataset per header after the first column.

Do NOT change the HTML structure, CSS, or Chart.js code. Only update the CSV data.